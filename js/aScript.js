/*const template = document.querySelector("template").content;
const section = document.querySelector("section");
const fetchLink = "mainPromos.json";
fetch(fetchLink)
  .then(res => res.json())
  .then(data => show(data));

function show(data) {
  data.forEach(render);
}

function render(aPromo) {
  let clone = template.cloneNode(true);
  console.log(aPromo.uno);
  clone.querySelector("h2").textContent = aPromo.uno;
  section.appendChild(clone);
}*/
